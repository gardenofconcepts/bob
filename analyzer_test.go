package main

import (
	/*"fmt"
	"os"
	"path/filepath"*/
	"testing"
)

func TestAnalyzer(t *testing.T) {

	t.Skip("Not implemented yet")

	/*baseDir := "/var/www"
	fileDir := "/var/www/src/bundle/werndl"
	globPath := "../../../web/**"

	globPath = filepath.Join(fileDir, globPath)

	t.Error("gp", globPath)

	globPath, _ = filepath.Rel(baseDir, globPath)

	t.Error("gp", globPath)

	fmt.Print(baseDir)

	pwd, err := os.Getwd()
	if err != nil {
		fmt.Println(err)
		os.Exit(1)
	}

	glob := "../../../builder/assets/**"

	pwd = filepath.Join(pwd, glob)

	t.Error("Path: ", pwd)

	hash, err := Analyzer("src/../assets/hash", []string{"**"}, []string{})

	if err != nil {
		t.Error("Error", err)
	}

	if hash != "er" {
		t.Error(hash)
	}*/
}
